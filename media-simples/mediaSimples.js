//
// Calcula a média simples (aritmética) de 3 valores quaisquer.
//

function mediaSimples(valor1, valor2, valor3) {
    return (valor1 + valor2 + valor3) / 3;
}
//console.log(mediaSimples(10, 9, 8))
module.exports.mediaSimples = mediaSimples;