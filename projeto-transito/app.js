
function verificarVelocidade(velocidade) {
    const velocidadeConstatada = velocidade - 7;

    if (velocidadeConstatada > 60)
        return true;
    else {
        return false;
    }

}

// module.exports = verificarVelocidade;
